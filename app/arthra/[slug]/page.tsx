import Link from 'next/link';
import { notFound } from 'next/navigation';
import { articles } from '../../../lib/articles';
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);return a?{title:a.title,description:a.excerpt}:{}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)return notFound();return <main><section className="page-hero"><div className="container article-detail"><div className="eyebrow">{a.category} · {a.date}</div><h1>{a.title}</h1><p className="lead">{a.excerpt}</p></div></section><section className="section"><div className="container article-detail"><div className="article-detail-content">{a.content.map((p,i)=><p key={i}>{p}</p>)}<h2>Σημαντικό</h2><p>Το περιεχόμενο είναι ενημερωτικό και δεν αντικαθιστά εξατομικευμένη αξιολόγηση από επαγγελματία υγείας.</p></div><div className="related"><div className="eyebrow">Σχετικά άρθρα</div><div className="article-grid" style={{marginTop:20}}>{articles.filter(x=>x.slug!==a.slug).slice(0,2).map(x=><Link className="card article" href={`/arthra/${x.slug}`} key={x.slug}><div className="article-visual"/><div className="article-body"><div className="article-meta">{x.category} · {x.date}</div><h3>{x.title}</h3></div></Link>)}</div></div></div></section></main>}
