import Link from 'next/link';import {notFound} from 'next/navigation';import {articles} from '../../../lib/articles';
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);return a?{title:a.title,description:a.excerpt}:{}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)return notFound();return <main><section className="page-hero"><div className="container article-detail"><div className="eyebrow">{a.category} · {a.date}</div><h1>{a.title}</h1><p>{a.excerpt}</p></div></section><section className="section"><div className="container article-detail" style={{position:'relative'}}><div aria-hidden="true" style={{position:'absolute',inset:'7% 8%',backgroundImage:'url(/logo.jpg)',backgroundRepeat:'no-repeat',backgroundPosition:'center',backgroundSize:'min(520px,72%)',opacity:.045,pointerEvents:'none',zIndex:0}}/><div style={{position:'relative',zIndex:1}}><div className="article-detail-visual" style={{backgroundImage:`url(${a.image})`}}/><div className="article-detail-content">{a.content.map((p,i)=><p key={i}>{p}</p>)}<h2>Μια χρήσιμη υπενθύμιση</h2><p>Η διατροφική πληροφορία χρειάζεται πλαίσιο. Όταν υπάρχουν συμπτώματα, χρόνια νοσήματα, σημαντικές διατροφικές αλλαγές ή πολλοί περιορισμοί, η αξιολόγηση πρέπει να είναι εξατομικευμένη.</p><div className="source-note">Το άρθρο αποτελεί σύγχρονη αναδιατύπωση/οργάνωση περιεχομένου από το προσωπικό αρχείο της Ευδοξίας στο <a href={a.sourceUrl} target="_blank" rel="noreferrer"><b>tsitakh.blogspot.com</b></a>.</div></div><div style={{marginTop:55}}><div className="eyebrow">Συνέχισε την ανάγνωση</div><div className="article-list-grid" style={{marginTop:18}}>{articles.filter(x=>x.slug!==a.slug).slice(0,3).map(x=><Link className="article-card-list" href={`/arthra/${x.slug}`} key={x.slug}><div className="article-visual" style={{backgroundImage:`url(${x.image})`}}><span>{x.category}</span></div><div className="article-body"><div className="article-meta">{x.date}</div><h3>{x.title}</h3></div></Link>)}</div></div></div></div></section></main>}
