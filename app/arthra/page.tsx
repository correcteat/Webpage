'use client';
import Link from 'next/link';import {useMemo,useState} from 'react';import {articles} from '../../lib/articles';
export default function Articles(){const [q,setQ]=useState('');const [cat,setCat]=useState('Όλα');const cats=['Όλα',...Array.from(new Set(articles.map(a=>a.category)))];const filtered=useMemo(()=>articles.filter(a=>(cat==='Όλα'||a.category===cat)&&`${a.title} ${a.excerpt}`.toLowerCase().includes(q.toLowerCase())),[q,cat]);return <main><section className="page-hero"><div className="container"><div className="eyebrow">Correct Eat Journal</div><h1>Άρθρα με ουσία, από το παλιό blog μέχρι σήμερα.</h1><p>Μεταφέρουμε και οργανώνουμε το εκπαιδευτικό αρχείο του προσωπικού blog της Ευδοξίας σε μια πιο σύγχρονη, εύχρηστη μορφή.</p></div></section><section className="section"><div className="container"><div className="searchbar"><input aria-label="Αναζήτηση άρθρων" placeholder="Αναζήτησε θέμα, π.χ. IBS, βάρος, υδατάνθρακες…" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="category-row">{cats.map(c=><button className={`btn ${cat===c?'primary':'ghost'}`} key={c} onClick={()=>setCat(c)}>{c}</button>)}</div><div className="article-list-grid">{filtered.map(a=><Link className="article-card-list" href={`/arthra/${a.slug}`} key={a.slug}><div className="article-visual" style={{backgroundImage:`url(${a.image})`}}><span>{a.category}</span></div><div className="article-body"><div className="article-meta">{a.date}</div><h3>{a.title}</h3><p>{a.excerpt}</p><span className="source-pill">Από το αρχείο blog</span></div></Link>)}</div>{filtered.length===0&&<p className="muted" style={{marginTop:30}}>Δεν βρέθηκε άρθρο.</p>}</div></section></main>}
