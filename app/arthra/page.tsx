'use client';
import Link from 'next/link';import {useMemo,useState} from 'react';import {articles} from '../../lib/articles';
export default function Articles(){const [q,setQ]=useState('');const [cat,setCat]=useState('Όλα');const cats=['Όλα',...Array.from(new Set(articles.map(a=>a.category)))];const filtered=useMemo(()=>articles.filter(a=>(cat==='Όλα'||a.category===cat)&&(`${a.title} ${a.excerpt}`.toLowerCase().includes(q.toLowerCase()))),[q,cat]);return <main><section className="page-hero"><div className="container"><div className="eyebrow">Άρθρα</div><h1>Γνώση που γίνεται πράξη.</h1><p>Το νέο blog του Correct Eat. Η υποδομή είναι έτοιμη ώστε να μεταφέρουμε σταδιακά το υπάρχον περιεχόμενο από το tsitakh.blogspot.com.</p></div></section><section className="section"><div className="container"><div className="searchbar"><input aria-label="Αναζήτηση άρθρων" placeholder="Αναζήτησε άρθρο…" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="category-row">{cats.map(c=><button className={`btn ${cat===c?'primary':'ghost'}`} key={c} onClick={()=>setCat(c)}>{c}</button>)}</div><div className="article-grid">{filtered.map(a=><Link className="card article" href={`/arthra/${a.slug}`} key={a.slug}><div className="article-visual"/><div className="article-body"><div className="article-meta">{a.category} · {a.date}</div><h3>{a.title}</h3><p>{a.excerpt}</p></div></Link>)}</div>{filtered.length===0&&<p className="muted">Δεν βρέθηκαν άρθρα με αυτά τα κριτήρια.</p>}</div></section></main>}
